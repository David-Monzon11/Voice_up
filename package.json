{
  "name": "ireportph",
  "version": "1.0.0",
  "description": "iReportPH static site",
  "license": "ISC",
  "scripts": {
    "build": "echo 'No build needed for static site'",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
